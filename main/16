#include <iostream>
using namespace std;

int main() {
    setlocale(LC_ALL, "Rus");
    int size;
    char texture;

    cout << "Размер: ";
    cin >> size;
    cout << "Текстура: ";
    cin >> texture;

    cout << endl << "Результат:" << endl << endl;

    for (int i = 0; i < size; i++) {
        for (int j = 0; j < size; j++) {
            if ((i + j) % 2 == 0) {
                cout << ". ";
            }
            else {
                cout << texture << " ";
            }
        }
        cout << endl;
    }

    return 0;
}
